import styled from 'styled-components'

import { ColorStyles } from '../../shared'

export const SubsectionTitleContainer = styled.p`
  font-size: 1.125rem;
  font-weight: 500;
  line-height: 25.5px;
  text-transform: uppercase;
  color: ${ColorStyles.neutral['60']};
`
